import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:paroquia_sao_lourenco/app/modules/pastorais/pastorais_page.dart';

main() {
  testWidgets('PastoraisPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(PastoraisPage(title: 'Pastorais')));
    //  final titleFinder = find.text('Pastorais');
    //  expect(titleFinder, findsOneWidget);
  });
}
